server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yunnote?serverTimezone=UTC&useUnicode=yes&characterEncoding=UTF-8&verifyServerCertificate=false&useSSL=true
    username: root
    password: 1234
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql:true


logging.level:
  org.hibernate.type: trace